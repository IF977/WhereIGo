<h1>Seus sabores prediletos</h1>
<p>Encontre estabelecimentos seus tipos de comida preferido. De quais você gosta?</p>
<%= flash_message.html_safe %>

<%= form_tag action: 'register_c_preferences_food_create', method: 'POST' do |f| %>

    <div class="wig-choices-container">
        <% @checkboxes.each_pair do |id, name| %>
            <div class="wig-pref-choice wig-food">
                <%= check_box("food", "type", {:multiple => true }, id, nil) %>
                <label><%= name %></label>
            </div>
        <% end %>
    </div>
    
    <div class="wig-pref-buttons">
        <%= submit_tag "Continuar", class: "wig-button" %>
    </div>
<% end %>